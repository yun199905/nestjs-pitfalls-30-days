# 複製這個檔案為 .env（放在 repo 根目錄）：
#   cp .env.example .env
#
# 注意：@nestjs/config 預設讀取 process.cwd()/.env，
# 而 `nest start <project>` 的 cwd 是 repo 根目錄，
# 所以 .env 要放在根目錄，不是放在 apps/<專案>/ 底下。

# Day 17
DB_NAME=:memory:
DB_LOGGING=false
